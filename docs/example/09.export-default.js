"use strict";

// Module
//
//

let fruit = '123';
let dessert = 'abc';

export default function dinner(fruit, dessert){
    console.log(`今天的晚餐是 ${fruit} and ${dessert}`);
}

/**
 * function dinner(fruit, dessert){
     console.log(`今天的晚餐是 ${fruit} and ${dessert}`);
 }
 * export {dinner as chef}
 */
